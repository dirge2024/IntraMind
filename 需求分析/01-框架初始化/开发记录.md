# 框架初始化 — 开发记录

## 时间：2026-05-05

### 状态：✅ 已完成

### 前置依赖
- JDK 17 已安装
- Maven 3.8+ 已安装
- ES 8.10.0：本地已安装（D:\Elasticsearch\elasticsearch-8.10.0），认证 elastic/wsy2642@
- MinIO：本地已安装，端点 localhost:9000，认证 dirge/wsy2642@
- Kafka：其他项目已有实例，暂不部署

### 决策记录
- 不用 Docker 部署中间件，ES/MinIO 直连本地已有实例
- ES 客户端用 elasticsearch-java 8.10.0，版本与本地实例精确匹配
- ES HTTPS + 自签名证书问题：代码层跳过校验，不动本地 ES 配置
- Kafka 多项目共用一个实例，Topic 用 `localrag.*` 前缀隔离
- docker-compose.yml 仅作备选，供其他开发者使用

### 开发记录

| 步骤 | 内容 | 状态 | 备注 |
|------|------|------|------|
| 1 | docker-compose.yml（备选） | ✅ | MinIO + Kafka + ES |
| 2 | 父 pom.xml | ✅ | Spring Boot 3.3.5, ES 8.10.0 |
| 3 | common 模块 | ✅ | Result, BaseException, GlobalExceptionHandler |
| 4 | storage 空骨架 | ✅ | pom + MinIO 依赖 + 包目录 |
| 5 | messaging 空骨架 | ✅ | pom + spring-kafka 依赖 + 包目录 |
| 6 | document 空骨架 | ✅ | pom + 依赖 storage |
| 7 | embedding 空骨架 | ✅ | pom + okhttp 依赖 |
| 8 | vector-store 空骨架 | ✅ | pom + elasticsearch-java |
| 9 | retrieval 空骨架 | ✅ | pom + 依赖 embedding/vector-store |
| 10 | llm 空骨架 | ✅ | pom + 依赖 retrieval |
| 11 | api 模块 | ✅ | 启动类 + health + CORS + application.yml |
| 12 | .env.example + .gitignore | ✅ | |
| 13 | mvn clean compile 验证 | ✅ | BUILD SUCCESS，10 模块全部通过 |
| 14 | 启动验证 | 🔲 | 待 ES/MinIO 配置正确后启动 |

### 遇到的问题
无
